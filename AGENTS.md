# Repository AGENTS.md

## Code Change Workflow

- After every code update, run the relevant CI or validation commands and confirm they pass.
- After every code update, run a successful build before considering the task complete.
- Once validation and build both succeed, commit the changes and push them to the remote repository.
